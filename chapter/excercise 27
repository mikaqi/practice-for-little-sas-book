** Compute body mass index using pounds and inches;
DATA bodymass;
	Weight = 150;
	Height = 68;
	BMI = (Weight / Height ** 2) * 703;
RUN;
PROC PRINT DATA=bodymass;
RUN;

* BMI equals to 22.804930796
* Weight, Height and BMI are all numerics with length equals to 8
